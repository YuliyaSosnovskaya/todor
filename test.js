//  const doSomething = new Promise((res, rej) => {
  // const obj = undefined;
  // obj.field();
  // res('good');
  // rej();
  // let sum = 2 + 2;
  // if (sum == 'воскр') {
  //   res(123);
  // throw new Error('some error during promise processing');
  // } else {
    // res('пизда');
  // }
// });

// doSomething
//   .then (() => {датьпизды()})
//   .catch((value) => {console.log(value)});
